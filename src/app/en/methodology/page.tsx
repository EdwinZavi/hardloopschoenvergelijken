import { EnglishTextPage } from "@/app/en/text-page";

export const metadata = {
  title: "Methodology | Hardloopschoenvergelijken.nl",
  description: "How hardloopschoenvergelijken.nl compares running shoes, scores trade-offs and keeps retailer offers separate."
};

export default function EnglishMethodologyPage() {
  return <EnglishTextPage pageKey="methodology" />;
}
